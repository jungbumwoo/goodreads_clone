type User {
    id: ID! @id @unique
    username: String! @unique
    email: String! @unique
    firstName: String @default(value: "")
    lastName: String
    keeplist: String!
    uploadlist: [Book]
    reviewlist: [Review]
}

type Book {
    itemId
    title
    author
    pubDate
    priceStandard
    coverSmallUrl
    publisher
    description
    stars
    gotreview   
}

type Review {
    reviewid: ID! @id @unique
    score: 
    context:

}

책검색

mutation
회원가입
책등록
평점, 후기 남기기
유저별 keeplist생성